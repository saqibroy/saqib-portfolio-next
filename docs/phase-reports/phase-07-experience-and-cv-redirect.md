# Phase 7 Completion Report — Experience and CV Redirect

**Branch:** `phase-7-experience-redirect` (stacked on Phase 6)

**Date:** 2026-07-24

## Delivered

- Added the server-rendered `/experience` route, using only the validated
  shared portfolio content for profile, roles, capabilities, education, and
  language information.
- Added labelled CV downloads with the ATS document as the primary action and
  the visual CV as the secondary option.
- Removed the legacy CV page and layout, updated homepage and navigation links,
  and added an `/cv` to `/experience` permanent redirect.
- Added canonical metadata for `/experience` and updated the content inventory
  to record the legacy route's completed disposition.

## Verification evidence

| Check | Result |
| --- | --- |
| Redirect | Browser test verifies `/cv` returns 308 with `/experience` as its location and reaches the destination |
| Downloads | Browser test verifies both labelled PDF links; request checks return HTTP 200 |
| Accessibility | Axe checks pass with zero violations for `/experience` in light and dark themes |
| Visual review | Desktop and 390 px mobile screenshots captured and inspected for readable hierarchy, cards, focus-compatible controls, and responsive layout |
| Quality gate | `lint`, `typecheck`, `test`, `test:e2e`, `test:a11y`, `build`, production audit, and diff check passed |

## Notes

The old generated `.next/dev` route metadata referred to the deleted `/cv`
source during the first local typecheck. It was safely regenerated by removing
only `.next/dev`; the source tree was not altered by that cleanup.

## Next phase

Phase 8 moves the writing experience to `/writing`, adds legacy blog redirects,
and performs the required line-by-line factual review of both existing posts.
